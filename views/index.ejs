<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sound of Revival - Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #8B4513 !important; }
        .btn-primary { background-color: #8B4513; border-color: #8B4513; }
        .btn-primary:hover { background-color: #A0522D; border-color: #A0522D; }
        .text-primary { color: #8B4513 !important; }
        .bg-primary { background-color: #8B4513 !important; }
        .department-card { transition: transform 0.2s; }
        .department-card:hover { transform: translateY(-5px); }
        footer { background-color: #2c3e50; color: white; }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-music me-2"></i>Sound of Revival
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/departments">Departments</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/photos">Photos</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <% if (user) { %>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                                <i class="fas fa-user me-1"></i><%= user.firstName %>
                            </a>
                            <ul class="dropdown-menu">
                                <% if (user.role === 'admin') { %>
                                    <li><a class="dropdown-item" href="/admin/dashboard">Admin Dashboard</a></li>
                                <% } else { %>
                                    <li><a class="dropdown-item" href="/member/dashboard">Dashboard</a></li>
                                <% } %>
                                <li><hr class="dropdown-divider"></li>
                                <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                            </ul>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/register">Register</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

<!-- Hero Section -->
<section class="bg-primary text-white py-5">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6">
                <h1 class="display-4 fw-bold mb-4">Welcome to Sound of Revival</h1>
                <p class="lead mb-4">A Christian band dedicated to worship, praise, and spreading God's love through music and fellowship.</p>
                <% if (!user) { %>
                    <a href="/auth/register" class="btn btn-light btn-lg me-3">Join Us Today</a>
                    <a href="/auth/login" class="btn btn-outline-light btn-lg">Member Login</a>
                <% } else { %>
                    <a href="<%= user.role === 'admin' ? '/admin/dashboard' : '/member/dashboard' %>" class="btn btn-light btn-lg">Go to Dashboard</a>
                <% } %>
            </div>
            <div class="col-lg-6 text-center">
                <i class="fas fa-music" style="font-size: 200px; opacity: 0.3;"></i>
            </div>
        </div>
    </div>
</section>

<!-- Departments Overview -->
<section class="py-5">
    <div class="container">
        <h2 class="text-center mb-5">Our Departments</h2>
        <div class="row">
            <% departments.forEach(department => { %>
                <div class="col-md-4 mb-4">
                    <div class="card department-card h-100 shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title text-primary">
                                <i class="fas fa-users me-2"></i><%= department.name %>
                            </h5>
                            <p class="card-text"><%= department.description %></p>
                            <% if (user) { %>
                                <a href="/member/departments" class="btn btn-primary btn-sm">Learn More</a>
                            <% } else { %>
                                <small class="text-muted">Register to join this department</small>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</section>

<!-- Recent Announcements - Only for registered users -->
<% if (user && announcements.length > 0) { %>
<section class="py-5 bg-light">
    <div class="container">
        <h2 class="text-center mb-5">Latest Announcements</h2>
        <div class="row">
            <% announcements.forEach(announcement => { %>
                <div class="col-md-6 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"><%= announcement.title %></h5>
                            <p class="card-text"><%= announcement.content.substring(0, 150) %>...</p>
                            <small class="text-muted">
                                By <%= announcement.author.firstName %> <%= announcement.author.lastName %>
                            </small>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</section>
<% } %>

<!-- Photo Gallery Preview -->
<section class="py-5">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6">
                <h2 class="mb-4">Capturing Our Ministry</h2>
                <p class="lead mb-4">See the beautiful moments of worship, fellowship, and service in our photo gallery.</p>
                <% if (user) { %>
                    <a href="/member/photos" class="btn btn-primary btn-lg">
                        <i class="fas fa-camera me-2"></i>View Photo Gallery
                    </a>
                <% } else { %>
                    <a href="/photos" class="btn btn-primary btn-lg">
                        <i class="fas fa-camera me-2"></i>View Photo Gallery
                    </a>
                    <p class="mt-2 text-muted small">
                        <i class="fas fa-lock me-1"></i>Photo gallery is available to registered members only
                    </p>
                <% } %>
            </div>
            <div class="col-md-6 text-center">
                <i class="fas fa-images" style="font-size: 150px; opacity: 0.3; color: #8B4513;"></i>
            </div>
        </div>
    </div>
</section>

<!-- Call to Action -->
<% if (!user) { %>
<section class="py-5 bg-primary text-white">
    <div class="container text-center">
        <h2 class="mb-4">Ready to Join Our Ministry?</h2>
        <p class="lead mb-4">Become part of our Christian community and serve God through music and fellowship.</p>
        <a href="/auth/register" class="btn btn-light btn-lg">Register Now</a>
    </div>
</section>
<% } %>
  
  <!-- Footer -->
    <footer class="mt-5 py-4" style="background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%); color: white;">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5><i class="fas fa-music me-2"></i>Sound of Revival</h5>
                    <p class="mb-2">Serving God through music and fellowship</p>
                    <div class="social-links">
                        <a href="#" class="text-white me-3"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-youtube"></i></a>
                        <a href="#" class="text-white"><i class="fas fa-envelope"></i></a>
                    </div>
                </div>
                <div class="col-md-4 text-center">
                    <h6>Quick Links</h6>
                    <ul class="list-unstyled">
                        <li><a href="/about" class="text-white-50 text-decoration-none">About Us</a></li>
                        <li><a href="/departments" class="text-white-50 text-decoration-none">Departments</a></li>
                        <li><a href="/photos" class="text-white-50 text-decoration-none">Photo Gallery</a></li>
                        <li><a href="/auth/register" class="text-white-50 text-decoration-none">Join Us</a></li>
                    </ul>
                </div>
                <div class="col-md-4 text-md-end">
                    <p class="mb-1">&copy; 2024 Sound of Revival. All rights reserved.</p>
                    <p class="mb-0 small text-white-50">
                        <i class="fas fa-code me-1"></i>Designed by <strong>Peter J</strong>
                    </p>
                </div>
            </div>
            <hr class="my-3" style="border-color: #34495e;">
            <div class="row">
                <div class="col-12 text-center">
                    <small class="text-white-50">
                        "Make a joyful noise unto the Lord, all ye lands." - Psalm 100:1
                    </small>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>