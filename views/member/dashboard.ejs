<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Member Dashboard - Sound of Revival</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .navbar-brand { font-weight: bold; color: #8B4513 !important; }
        .btn-primary { background-color: #8B4513; border-color: #8B4513; }
        .btn-primary:hover { background-color: #A0522D; border-color: #A0522D; }
        .text-primary { color: #8B4513 !important; }
        .bg-primary { background-color: #8B4513 !important; }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-music me-2"></i>Sound of Revival
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/about">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/departments">Departments</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/member/photos">Photos</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user me-1"></i><%= user.firstName %>
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/member/dashboard">Dashboard</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/auth/logout">Logout</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

<div class="container py-4">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">
                <i class="fas fa-tachometer-alt me-2"></i>Welcome, <%= user.firstName %>!
            </h1>
        </div>
    </div>
    
    <div class="row">
        <!-- My Departments -->
        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header">
                    <h5><i class="fas fa-users me-2"></i>My Departments</h5>
                </div>
                <div class="card-body">
                    <% if (userDetails.departments.length > 0) { %>
                        <div class="row">
                            <% userDetails.departments.forEach(dept => { %>
                                <div class="col-md-6 mb-3">
                                    <div class="card border-primary">
                                        <div class="card-body">
                                            <h6 class="card-title"><%= dept.name %></h6>
                                            <p class="card-text small"><%= dept.description %></p>
                                            <form method="POST" action="/member/leave-department/<%= dept._id %>" class="d-inline">
                                                <button type="submit" class="btn btn-outline-danger btn-sm">Leave</button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <p class="text-muted">You haven't joined any departments yet.</p>
                        <a href="/member/departments" class="btn btn-primary">Browse Departments</a>
                    <% } %>
                </div>
            </div>
            
            <!-- Announcements -->
            <div class="card">
                <div class="card-header">
                    <h5><i class="fas fa-bullhorn me-2"></i>Announcements</h5>
                </div>
                <div class="card-body">
                    <% if (announcements.length > 0) { %>
                        <% announcements.forEach(announcement => { %>
                            <div class="border-bottom pb-3 mb-3">
                                <h6 class="mb-1">
                                    <%= announcement.title %>
                                    <% if (announcement.priority === 'high') { %>
                                        <span class="badge bg-danger">High Priority</span>
                                    <% } %>
                                </h6>
                                <p class="mb-1"><%= announcement.content %></p>
                                <small class="text-muted">
                                    By <%= announcement.author.firstName %> <%= announcement.author.lastName %>
                                    <% if (announcement.department) { %>
                                        in <%= announcement.department.name %>
                                    <% } %>
                                </small>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <p class="text-muted">No announcements at this time.</p>
                    <% } %>
                </div>
            </div>
        </div>
        
        <!-- Quick Actions -->
        <div class="col-md-4">
            <div class="card">
                <div class="card-header">
                    <h5><i class="fas fa-bolt me-2"></i>Quick Actions</h5>
                </div>
                <div class="card-body">
                    <div class="d-grid gap-2">
                        <a href="/member/departments" class="btn btn-outline-primary">
                            <i class="fas fa-users me-2"></i>Browse Departments
                        </a>
                        <a href="/departments" class="btn btn-outline-info">
                            <i class="fas fa-info-circle me-2"></i>View All Departments
                        </a>
                        <a href="/member/photos" class="btn btn-outline-info">
                            <i class="fas fa-camera me-2"></i>Photo Gallery
                        </a>
                        <a href="/about" class="btn btn-outline-secondary">
                            <i class="fas fa-question-circle me-2"></i>About SOR
                        </a>
                    </div>
                </div>
            </div>
            
            <!-- Member Stats -->
            <div class="card mt-3">
                <div class="card-header">
                    <h5><i class="fas fa-chart-bar me-2"></i>My Stats</h5>
                </div>
                <div class="card-body">
                    <p><strong>Departments Joined:</strong> <%= userDetails.departments.length %></p>
                    <p><strong>Member Since:</strong> <%= new Date(userDetails.joinedDate).toLocaleDateString() %></p>
                </div>
            </div>
        </div>
    </div>
</div>    
<!-- Footer -->
    <footer class="mt-5 py-4" style="background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%); color: white;">
        <div class="container">
            <div class="row">
                <div class="col-md-4">
                    <h5><i class="fas fa-music me-2"></i>Sound of Revival</h5>
                    <p class="mb-2">Serving God through music and fellowship</p>
                    <div class="social-links">
                        <a href="#" class="text-white me-3"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="text-white me-3"><i class="fab fa-youtube"></i></a>
                        <a href="#" class="text-white"><i class="fas fa-envelope"></i></a>
                    </div>
                </div>
                <div class="col-md-4 text-center">
                    <h6>Member Area</h6>
                    <ul class="list-unstyled">
                        <li><a href="/member/dashboard" class="text-white-50 text-decoration-none">Dashboard</a></li>
                        <li><a href="/member/departments" class="text-white-50 text-decoration-none">My Departments</a></li>
                        <li><a href="/member/photos" class="text-white-50 text-decoration-none">Photo Gallery</a></li>
                        <li><a href="/about" class="text-white-50 text-decoration-none">About Us</a></li>
                    </ul>
                </div>
                <div class="col-md-4 text-md-end">
                    <p class="mb-1">&copy; 2024 Sound of Revival. All rights reserved.</p>
                    <p class="mb-0 small text-white-50">
                        <i class="fas fa-code me-1"></i>Designed by <strong>Peter J</strong>
                    </p>
                </div>
            </div>
            <hr class="my-3" style="border-color: #34495e;">
            <div class="row">
                <div class="col-12 text-center">
                    <small class="text-white-50">
                        "Make a joyful noise unto the Lord, all ye lands." - Psalm 100:1
                    </small>
                </div>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>